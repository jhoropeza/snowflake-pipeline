# Controls when the action will run.
name: ❄️ Despliegue de Notebooks y Fetch

on:
  # Ejecución automática al hacer push en main (para despliegue PROD automático)
  push:
    branches:
      - main

  # Permite ejecutar este workflow manualmente desde la pestaña Actions
  workflow_dispatch:
    inputs:
      deployment_target: # Nombre de la variable de entrada
        description: 'Selecciona la rama/entorno de despliegue'
        required: true
        type: choice
        options: # Opciones que aparecerán en el botón "Run workflow"
          - DEV
          - MAIN

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python packages
        run: pip install -r requirements.txt

      # 1. Configura la conexión y ejecuta la lógica
      - name: Deploy notebooks and fetch
        # Se define una variable para la rama seleccionada. Si es ejecución manual (workflow_dispatch),
        # toma el valor de la entrada. Si es 'push' automático, usa 'main'.
        # La variable env_target se usará para ambos: la rama de Git y el esquema de destino (env).
        env:
          ENV_TARGET: ${{ github.event_name == 'push' && 'MAIN' || github.event.inputs.deployment_target }}
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
          
        run: |
          # 2. Convertir a minúsculas para usar en la URL de la rama (e.g., MAIN -> main)
          BRANCH_NAME=$(echo $ENV_TARGET | tr '[:upper:]' '[:lower:]')

          # 3. Primer comando: Fetch de la rama seleccionada
          # Esto garantiza que la versión de Git sea la más reciente
          snow sql -q "ALTER GIT REPOSITORY DEMO_GIT_REPO_V2 FETCH" --temporary-connection --account $SNOWFLAKE_ACCOUNT --user $SNOWFLAKE_USER --role $SNOWFLAKE_ROLE --warehouse $SNOWFLAKE_WAREHOUSE --database $SNOWFLAKE_DATABASE --schema $SNOWFLAKE_SCHEMA

          # 4. Segundo comando: Ejecutar el script SQL usando las variables dinámicas
          # env => $ENV_TARGET (ej: PROD) y branch => $BRANCH_NAME (ej: main)
          snow sql -q "EXECUTE IMMEDIATE FROM @DEMO_GIT_REPO_V2/branches/$BRANCH_NAME/script_to_deploy_notebooks/deploy_notebooks.sql USING (env => '$ENV_TARGET', branch => '$BRANCH_NAME')" --temporary-connection --account $SNOWFLAKE_ACCOUNT --user $SNOWFLAKE_USER --role $SNOWFLAKE_ROLE --warehouse $SNOWFLAKE_WAREHOUSE --database $SNOWFLAKE_DATABASE --schema $SNOWFLAKE_SCHEMA
